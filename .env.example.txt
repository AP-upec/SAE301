# Copier ce fichier en .env et remplir les valeurs.
# Le fichier .env ne doit JAMAIS être versionné (il est dans .gitignore).

# Base de données MySQL
DB_USER=sae-301
DB_PASSWORD=mot_de_passe
DB_HOST=
DB_NAME=sae-301_db